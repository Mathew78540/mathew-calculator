import React from 'react';

// Styles
import './styles.css';

/**
 * Button
 * 
 * @param {object} props
 */
const Button = (props) => (
  <button {...props} className="button" />
);

export default Button;
